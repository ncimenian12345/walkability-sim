{
  "name": "walkability-sim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build && SITE_PASSWORD=${SITE_PASSWORD:-test} PORT=4173 node server.mjs",
    "fetch-town": "node scripts/fetch-town.mjs",
    "test": "node --test tests/*.test.mjs",
    "start": "node server.mjs"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  },
  "engines": {
    "node": ">=20"
  }
}
